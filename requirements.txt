# Core runtime — dependency-light and deployment-neutral by design.
# The engine (library + CLI) depends only on a config parser, data models, and an
# optional HTTP client; it pulls in NO web framework and NO provider SDK. The LLM
# judge's provider client (e.g. `anthropic`) is lazy-imported only when a live judge
# is selected, so the hermetic engine + test suite run without it.
pyyaml>=6.0
pydantic>=2.13.4
httpx>=0.27

# Optional, lazy-imported document parsers (not required for the hermetic engine):
#   pypdf>=4.0          # PDF extraction
#   python-docx>=1.1    # DOCX extraction
# The bundled PDF renderer is dependency-free (emits a minimal valid PDF).
