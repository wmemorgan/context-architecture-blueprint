# Development + CI dependencies.
# Pulls in the reference-service requirements (which in turn pull in the core), so a
# single `pip install -r requirements-dev.txt` provisions everything needed to run the
# full test suite (engine core + reference-service example) plus lint/format/type gates.
-r examples/reference-service/requirements.txt
pytest>=9.1.1
ruff>=0.15.20
black>=24.0
mypy>=2.3.0
types-PyYAML>=6.0.12.20260518
